import React from 'react';import styled from "styled-components";import {FlexWrapper} from "../../../../components/FlexWrapper";import {Button} from "../../../../components/button/Button";type WorkProps = {    title: string    description: string    technologies: string[]    image: string}export const Work = (props: WorkProps) => {    return (        <StyledWork>            <Image src={props.image} alt={props.title}/>            <Title>                {props.title}            </Title>            <FlexWrapper justifyContent={'flex-start'}>                {props.technologies.map(el => {                    return <Button group={"technologies"} name={el} />                })}            </FlexWrapper>            <Text>                {props.description}            </Text>            <FlexWrapper>                <Button group={"technologies"} name={'Demo'}/>                <Button group={"technologies"} name={'Code'}/>            </FlexWrapper>        </StyledWork>    );};const StyledWork = styled.div`  background-color: #c9f5c3;`const Image = styled.img`  max-width: 430px;  height: 260px;  width: 100%;  object-fit: cover;`const Title = styled.h4``const Text = styled.p``